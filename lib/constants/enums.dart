enum LoginType { kakao, google, apple, email }

enum ActivityType {
  climbing,
}

enum WalletType {
  inventory,
  asset,
}

enum AssetType {
  token,
  coin,
  nft,
}

enum WalletActionType {
  recharge,
  sendToAsset,
  receive,
  sendToInventory,
  sendOutside,
}

enum HiveKey { accessToken, refreshToken }
